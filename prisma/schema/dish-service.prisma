model DishService {
    id        Int       @id @default(autoincrement())
    title     String
    slug      String
    dishs     Dish[]
    pictures  Picture[]
    updatedAt DateTime  @default(now())
    createdAt DateTime  @default(now())
}
