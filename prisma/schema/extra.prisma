model Extra {
  id           Int                    @id @default(autoincrement())
  labelFR      String                 @unique
  labelEN      String                 @unique
  dishServices DishServicesOnExtras[]
  updatedAt    DateTime               @default(now())
  createdAt    DateTime               @default(now())
}
